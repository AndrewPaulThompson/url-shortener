.form {
    display: flex;
    justify-content: center;
    align-items: center;
    flex-flow: column;
    font-family: Arial, Helvetica, sans-serif;

    &__title {
        h1 {
            font-size: 1.5em;
            
        }
    }

    &__success {
        display: none;
    }
}